Replace CLAUDE.md with the following content, then create docs/euc-context.md, docs/data-dictionary# EUC Data Analysis Project

## Quick Commands
- `uv run pytest` - Run data quality tests
- `uv run jupyter lab` - Launch notebooks
- `uv sync` - Install/update dependencies

## Data Handling Rules (CRITICAL)
- NEVER display raw PII (names, SSNs, addresses, DOB)
- Always use `src/data/anonymize.py` before showing data samples
- Minimum cell size of 10 for any aggregated statistics
- Data files live in `data/` and are NEVER committed to git

## Analysis Workflow
1. Raw data → `data/raw/` (read-only)
2. Run cleaning pipeline → `data/processed/`
3. Analysis in notebooks or src/analysis/
4. Results → `outputs/` (PII-free)

## Project Structure
- `data/raw/` - Original data files (never modify)
- `data/processed/` - Cleaned, de-identified working data
- `notebooks/` - Jupyter notebooks for analysis
- `src/data/` - Data loading, cleaning, anonymization
- `src/analysis/` - Descriptive, impact, and predictive analysis
- `outputs/` - Figures, tables, reports (no PII)

## Code Conventions
- Type hints on all functions
- Docstrings for public functions
- pandas for data manipulation
- statsmodels for causal inference (diff-in-diff, regression)
- scikit-learn for predictive models
